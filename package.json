{
  "name": "stray-server",
  "version": "1.0.0",
  "description": "test of using express to produce rest api",
  "main": "stray-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
